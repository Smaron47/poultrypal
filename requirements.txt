Flask==3.0.2
tensorflow      # CRITICAL: Barebones CPU distribution saves ~400MB RAM
numpy==1.26.4
opencv-python-headless==4.9.0.80  # CRITICAL: Bypasses server GUI package dependencies
Pillow==10.2.0
matplotlib==3.8.3
gunicorn==21.2.0            # High-performance production web server
